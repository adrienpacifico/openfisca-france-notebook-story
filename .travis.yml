language: python
python: 3.6
install:
  - pip install -r requirements.txt
# command to run tests
script:
  - jupyter nbconvert --to notebook --execute notebooks/comment_fonctionne_l_Impot_sur_le_revenu_francais.ipynb